fix: Resolve theme toggle functionality

- Corrected the theme toggle logic in `+layout.svelte` to properly interact with the `.dark` class defined in `app.css`.
- Changed `data-theme` attribute manipulation to `document.documentElement.classList.toggle('dark')` for effective theme switching.