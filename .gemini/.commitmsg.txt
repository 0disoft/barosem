fix: Resolve theme toggle icon visibility in dark mode

- Corrected `class:` directive usage in Svelte components by dynamically constructing class strings.
- Ensured proper icon visibility and smooth transitions in both light and dark themes using opacity and rotation.